name: telegram_recipe_bot
description: A Telegram bot for managing culinary recipes using teledart and local persistence.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  teledart: ^0.2.14
  dotenv: ^3.1.2
